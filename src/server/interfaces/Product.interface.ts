export interface IProduct {
  active?: boolean;
  boxsize: number;
  description: string;
  depth: number;
  imageFilesData: object[];
  length: number;
  package: number;
  title: string;
  weight: number;
  wide: number;
}
