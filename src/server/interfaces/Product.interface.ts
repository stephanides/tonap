export interface IProduct {
  boxsize: number;
  description: string;
  depth: number;
  imageFilesData: object[];
  length: number;
  package: number;
  title: string;
  weight: number;
  wide: number;
}
